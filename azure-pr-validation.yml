trigger: none

pool:
  vmImage: ubuntu-latest

variables:
  - group: Semgrep_Variables

stages:
  - stage: PRValidation
    displayName: Semgrep PR validation

    jobs:
      - template: templates/security-scan.yml
        parameters:
          defaultBranch: main

          